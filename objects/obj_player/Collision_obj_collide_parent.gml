/// @description Quick fix collider

jump = false;
speed = 0;
gravity = 0;
landing_angle = image_angle;

/*if(landing_angle > 180 && !angle_reset) {
	landing_angle = 360 - landing_angle;
	ang = landing_angle;

	
} else if (landing_angle < 180 && !angle_reset){
	landing_angle = landing_angle + 360;
	ang = landing_angle;

}*/

